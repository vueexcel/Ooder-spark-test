const state = {
  allProducts: []
}

const getters = {
    getAllProducts(){ return state.allProducts; }
}

const mutations = {
    
}

const actions = {
    
}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
}